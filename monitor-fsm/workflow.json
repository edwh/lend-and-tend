{
  "id": "freegle-monitor-v2",
  "name": "Freegle Monitor",
  "version": "2",
  "description": "FSM-enforced freegle-monitor iteration. Fully parallel architecture: CHECK_CI gathers master CI status, red PRs, and active Discourse topics in one tool node, then CI_ROUTER dispatches to either FIX_MASTER_CI (master red) or PARALLEL_ANALYZE_AND_FIX (master green). PARALLEL_ANALYZE_AND_FIX launches all PR fixes, all Discourse topic analyses, and Sentry analysis simultaneously in isolated worktrees. COLLATE_RESULTS merges classifications and Sentry data, then routes to WORK_ROUTER for bug/Sentry/coverage dispatch. Bug fixes use a TDD pipeline: DIAGNOSE_BUG (Opus writes structured brief) → REPRODUCE_BUG (sub-agent writes failing test) → REVIEW_REPRODUCTION (Opus gate: test matches symptom?) → IMPLEMENT_FIX (sub-agent makes test pass, pushes PR) → VERIFY_DISCOURSE_BATCH (adversarial review). Reaching END requires passing COVERAGE_GATE (at least one PR since iterationStartTs).",
  "initialState": "LOAD_STATE",
  "guardrails": "You are running inside an FSM. You MUST return JSON matching the LLMDecision schema exactly. The response MUST be a single JSON object with these keys: reasoning (string), contextUpdates (OBJECT, not a string — use {} for no updates, NEVER write \"contextUpdates\": \"{...}\"), actions (ARRAY, not a string — use [] for no actions), proposedTransition (string or null). Do NOT wrap the whole response in markdown code fences. Do NOT JSON-stringify nested fields. You may only call actions listed for the current state, and may only propose transitions listed as 'Valid Next States'. You cannot bypass the PR gate: reaching END requires a PR (bug-fix or coverage) to have been created this iteration, verified by verify_pr_created. Do not lie about actions you have not taken. HARD INVARIANT — NEVER DISMISS A RED CHECK: A failing CI run, failing test, or red status check on ANY PR @me authored is ALWAYS a real bug that must be investigated to root cause and fixed. It does NOT matter whether the failure looks related to the change or not. You MUST NOT describe a failure as 'flaky', 'environmental', 'transient', 'pre-existing', 'unrelated', 'infrastructure timing', 'polluted test data', or any equivalent. You MUST NOT 'rerun and hope'. You MUST NOT mark such a failure as 'deferred'. Deferred is reserved EXCLUSIVELY for Discourse bugs and Sentry issues that need external information (reporter clarification, hardware the host cannot access, a user judgment call). CI failures never qualify — they are always within reach and must be driven to green.",
  "states": {
    "LOAD_STATE": {
      "description": "Load state.json, record iterationStartTs. Start node — ai-flower requires exactly one 'start' nodeType, so we keep this as 'start' but the driver treats start nodes with readActions the same as tool nodes (deterministic, no LLM turn).",
      "nodeType": "start",
      "readActions": [
        "load_state",
        "sync_pr_states",
        "check_bug_feedback",
        "queue_deployed_reply_drafts"
      ],
      "writeActions": []
    },
    "CHECK_CI": {
      "description": "Pre-parallel discovery: check master CI, red PRs, and which Discourse topics have new posts. All three run in this single tool node before the parallel dispatch. Results feed CI_ROUTER.",
      "nodeType": "tool",
      "readActions": [
        "check_master_ci",
        "check_my_open_pr_ci",
        "discover_active_topics"
      ],
      "writeActions": []
    },
    "CI_ROUTER": {
      "description": "Dispatch based on master CI status. Tool node — ci_router_decide returns _transition deterministically. Priority: (1) master red → FIX_MASTER_CI; (2) master green → PARALLEL_ANALYZE_AND_FIX (red PRs are fixed in parallel alongside Discourse/Sentry analysis).",
      "nodeType": "tool",
      "readActions": [
        "ci_router_decide"
      ],
      "writeActions": []
    },
    "FIX_MASTER_CI": {
      "description": "Fix a failing master CI run. Direct-push to master permitted (CI-failure exception). Exactly one job: diagnose, fix, push, mark done. Returns to CHECK_CI to re-verify before Phase A can exit.",
      "nodeType": "agent",
      "prompt": "Master CI is failing on runId context._action_check_master_ci.latestRun.databaseId. (1) Call fetch_ci_failure_logs with that runId to get the failure. (2) Call delegate_to_coder with a task describing the failure and asking it to diagnose, fix, and push directly to master. Include the log tail and the sha. The subagent MUST identify a concrete root cause — never accept 'flake', 'transient', 'environmental', or 'unrelated' as a fix. (3) When delegate_to_coder returns, call create_pr if a PR was opened OR set contextUpdates.masterFixAttempted = true if pushed direct to master. Propose transition to CHECK_CI so Phase A re-reads CI status before deciding whether to advance.",
      "readActions": [
        "fetch_ci_failure_logs",
        "search_code"
      ],
      "writeActions": [
        "delegate_to_coder",
        "create_pr"
      ]
    },
    "PARALLEL_ANALYZE_AND_FIX": {
      "description": "The core parallel dispatch. Launches ONE Claude agent for the single focus PR (to avoid flooding the single self-hosted runner queue), one per active Discourse topic (triage), and one Sentry analysis — all simultaneously. Focusing on one PR at a time drives it to green before touching the next, rather than making marginal progress on all.",
      "nodeType": "agent",
      "prompt": "You are about to launch the parallel work batch for this iteration. All tasks run simultaneously.\n\nINPUTS:\n- context._action_ci_router_decide.focusPRNumber — the ONE PR to fix this iteration (null if none / drain mode)\n- context._action_ci_router_decide.exhaustedPRNumbers — PR numbers that have hit the 3-attempt budget; these are already excluded from focusPRNumber\n- context._action_check_my_open_pr_ci.redPRs — all red PRs (for context/logging only — do NOT launch fix tasks for them)\n- context._action_discover_active_topics.topics — discourse topics; filter to those with hasNew: true\n\nBUILD YOUR TASKS ARRAY:\n\n(A) At most ONE PR fix task. If context._action_ci_router_decide.focusPRNumber is null, skip section A entirely.\n  Otherwise create exactly one task with id 'pr-<focusPRNumber>':\n  Task: 'Fix CI failure on PR #<n>. (a) Run `gh pr checkout <n> -R Freegle/Iznik`. (b) Read the failing check URLs from the provided failedChecks[]. For CircleCI use the v1.1 API with CIRCLECI_TOKEN from ~/.circleci/cli.yml per .circleci/README.md. (c) Identify a CONCRETE root cause — never accept flaky/transient/unrelated. (d) Fix in code. (e) Push a new commit to the PR branch. Emit COMMIT_PUSHED=<sha> when done.'\n  IMPORTANT: Only fix the focus PR. Do NOT create fix tasks for other red PRs — they wait their turn. Fixing multiple PRs simultaneously floods the single CI runner queue and causes thrashing.'\n\n\nCRITICAL GATE — CHECK BEFORE BUILDING TASKS:\nIf context._action_ci_router_decide.onlyFixPR === true:\n  - Build ONLY section (A): the single focus PR fix task.\n  - SKIP sections (B) and (C) entirely.\n  - Reason: we have a red PR to drive to green. Launching Discourse triage or Sentry\n    analysis while CI is pending wastes tokens on work we cannot act on — no new PRs\n    should be created until the focus PR is resolved. Token budget is finite.\n  - Call delegate_parallel_tasks with ONLY the PR task (or an empty array if focusPRNumber is null).\n  - Do not justify skipping triage — it is correct behaviour, not an error.\n\nIf context._action_ci_router_decide.onlyFixPR === false (or absent):\n  - Build all applicable sections below.\n\n(B) One task per active Discourse topic (id: 'topic-<id>') for each topic with hasNew === true:\n  Task: 'Analyse Discourse topic <id> for new bug reports. This is an analysis-only task — no code changes.\n\n  STEP 1. Get API key:\n    python3 -c \"import json; print(json.load(open(\\'/home/edward/profile.json\\'))[\\'auth_pairs\\'][0][\\'user_api_key\\'])\"\n\n  STEP 2. Get cursor from DB:\n    sqlite3 /home/edward/FreegleDockerWSL/monitor-fsm/monitor.db \"SELECT COALESCE((SELECT last_post_number FROM topic_cursor WHERE topic_id=<id>),0)\"\n\n  STEP 3. Fetch topic metadata (first page + stream array):\n    curl -s -H \"User-Api-Key: <key>\" -H \"Api-Username: Edward_Hibbert\" https://discourse.ilovefreegle.org/t/<id>.json\n    From the JSON, read:\n      - highest_post_number (or posts_count if absent) — the true highest post number\n      - post_stream.stream — array of ALL post IDs in order (stream[0]=ID of post#1, stream[1]=ID of post#2, etc.)\n    If highest_post_number <= cursor: update cursor to highest_post_number, emit CLASSIFICATIONS=[] then ANALYSIS_COMPLETE=no new posts, and stop.\n\n  STEP 4. Fetch new post content via the posts endpoint (NOT by re-fetching the topic):\n    IMPORTANT: deleted posts shrink stream.length below highest_post_number, so you CANNOT use cursor as a direct stream index.\n    Correct calculation:\n      stream_len = len(post_stream.stream)\n      deleted_count = highest_post_number - stream_len   # may be 0\n      start_index = stream_len - (highest_post_number - cursor)   # position of first new post in stream\n      start_index = max(0, start_index)\n      New post IDs = post_stream.stream[start_index : start_index + 100]\n    If New post IDs is empty, there are no new posts — emit CLASSIFICATIONS=[] then ANALYSIS_COMPLETE=no new posts, and stop.\n    Fetch in batches of 50:\n      curl -s -H \"User-Api-Key: <key>\" -H \"Api-Username: Edward_Hibbert\" \"https://discourse.ilovefreegle.org/t/<id>/posts.json?post_ids[]=<id1>&post_ids[]=<id2>...\"\n    Each response has post_stream.posts[] with post_number, username, cooked (HTML body).\n    Use python3 to strip HTML tags from cooked for readable text: import re; re.sub(\\'<[^>]+>\\', \\' \\', cooked)\n\n  STEP 5. Recent commits: git -C /home/edward/FreegleDockerWSL log --oneline --since=\\'3 days ago\\'\n\n  STEP 6. Classify each fetched post with these fields:\n  - type: bug/retest/confirmed/already_fixed/question/off_topic/mine (mine = Edward_Hibbert posts only)\n  - featureArea: short specific label like \\'iOS chitchat post loading\\'\n  - summary: one sentence describing the issue\n  - originalPostText: first 1200 chars of stripped text\n  - user: username\n  - symptom_tags: (bugs/retest only) 3-8 lowercase keyword tags for the specific observable symptom, e.g. [\\'delete\\',\\'404\\',\\'stdmsg\\']. Focus on WHAT fails, not which feature.\n  - code_area: (bugs/retest only) \\'layer:Component\\' where layer is one of: go-api, nuxt, php, infra. Component is the handler/component most likely at fault, e.g. \\'go-api:DeleteStdMsg\\', \\'nuxt:ModStdMsg\\'. Omit if genuinely uncertain.\n\n  STEP 7. Update cursor to the highest post_number seen across all fetched posts:\n    sqlite3 /home/edward/FreegleDockerWSL/monitor-fsm/monitor.db \"INSERT INTO topic_cursor (topic_id, last_post_number, title, updated_at) VALUES (<id>, <maxPostNumber>, \\'<title>\\', datetime(\\'now\\')) ON CONFLICT(topic_id) DO UPDATE SET last_post_number=excluded.last_post_number, title=COALESCE(excluded.title,title), updated_at=excluded.updated_at\"\n\n  STEP 8. Output CLASSIFICATIONS=[{json array}] — single line, valid JSON. Empty array is valid.\n  Then output ANALYSIS_COMPLETE=<n> posts classified in topic <id>.\n  Do NOT emit DELEGATE_FAILED — finding no bugs is a valid successful outcome.'\n\n(C) One Sentry task (id: 'sentry'):\n  Task: 'List unresolved Sentry issues. This is an analysis-only task — no code changes.\n  1. SENTRY_AUTH_TOKEN: grep SENTRY_AUTH_TOKEN /home/edward/FreegleDockerWSL/.env | cut -d= -f2\n  2. For each project (nuxt3, go): curl -s -H \"Authorization: Bearer <token>\" \"https://sentry.io/api/0/projects/freegle/<project>/issues/?query=is:unresolved&sort=date&limit=10\"\n  3. Output exactly: SENTRY_ISSUES=[{\"project\":\"nuxt3\",\"id\":\"...\",\"title\":\"...\",\"count\":N,\"permalink\":\"...\"},...] — single line, valid JSON. Empty array is valid.\n  4. Then output ANALYSIS_COMPLETE=sentry scan done.\n  Do NOT emit DELEGATE_FAILED — finding no issues is a valid successful outcome.'\n\nCALL delegate_parallel_tasks with the complete tasks array. Do NOT omit any active topics — the whole point is that they run simultaneously with the PR fixes.\n\nAfter delegate_parallel_tasks returns, propose transition to COLLATE_RESULTS.",
      "readActions": [],
      "writeActions": [
        "delegate_parallel_tasks"
      ]
    },
    "COLLATE_RESULTS": {
      "description": "Merge results from the parallel dispatch into FSM context: parse CLASSIFICATIONS from topic delegates, SENTRY_ISSUES from the sentry delegate, and record PR fix outcomes. Then route to WORK_ROUTER.",
      "nodeType": "agent",
      "prompt": "Process context._action_delegate_parallel_tasks.results[].\n\nFor each result:\n  - id starts with 'topic-': extract the numeric topic id from the id (e.g. 'topic-9518' → 9518). Search stdoutTail for CLASSIFICATIONS=[...]. Parse the JSON array. For each entry in the array: (a) set entry.topic = <extracted topic id>, (b) if entry has post_number but no post, set entry.post = entry.post_number. Then merge the annotated entries into contextUpdates.classifications (append, not replace).\n  - id starts with 'pr-': the result is a PR CI fix attempt. Append { prNumber: <n>, exitCode, pushed, timedOut } to contextUpdates.openPRFixAttempts.\n  - id === 'sentry': search stdoutTail for SENTRY_ISSUES=[...]. Parse the JSON array. Set contextUpdates.sentryIssues = parsed array.\n\nAfter processing all results:\n  - contextUpdates.classifications should be the merged flat array with topic+post on every entry.\n  - contextUpdates.sentryIssues should be set (may be empty).\n  - If any classifications were parsed: call persist_classifications to persist them to the DB.\n  - Log a brief summary: how many classifications, how many bugs/retests, how many sentry issues, how many PR fixes succeeded.\n\nPropose transition to WORK_ROUTER.",
      "readActions": [],
      "writeActions": [
        "persist_classifications"
      ]
    },
    "WORK_ROUTER": {
      "description": "Phase B dispatch. Tool node — work_router_decide branches deterministically on context.classifications + context.bugsFixed + context.sentryIssues. Priority: (1) unfixed 'bug'/'retest' → DISPATCH_ALL_BUGS; (2) actionable Sentry issues → FIX_SENTRY_ISSUE; (3) else → COVERAGE_GATE.",
      "nodeType": "tool",
      "readActions": [
        "persist_classifications",
        "work_router_decide"
      ],
      "writeActions": []
    },
    "DIAGNOSE_BUG": {
      "description": "TDD pipeline step 1: Opus writes a structured diagnosis brief for the single focus bug. Does NOT dispatch any sub-agent — pure reasoning over the bug report and codebase. Output is stored in context for REPRODUCE_BUG.",
      "nodeType": "agent",
      "prompt": "TWO-PHASE STATE — check context first to determine which phase to execute.\n\n═══════════════════════════════════════════════════════════\nPHASE 1 (GATHER) — execute this if context._action_search_code is NOT set\n═══════════════════════════════════════════════════════════\n\nDo NOT write a diagnosis. Do NOT produce a diagnosisBrief. Gather evidence first.\n\nINPUT: context._action_work_router_decide.singleBug\nFields: { topic, post, user/reporter, excerpt/summary, featureArea, symptomTags, codeArea, originalPostText }\n\nRead the bug report (originalPostText is the full text; excerpt/summary is a one-liner).\nDetermine:\n  a) check_existing_prs params: bugDescription = excerpt/summary; extraKeywords = component names from codeArea (e.g. 'DeleteStdMsg', 'AIImageReview')\n  b) search_code params: query = the component/function name from codeArea; location = the relevant directory\n     (codeArea pattern → e.g. 'go-api:DeleteStdMsg' → query='DeleteStdMsg', location='iznik-server-go'\n                              'nuxt:AIImageReview'  → query='AIImageReview', location='iznik-nuxt3'\n                              'php:MembershipClass' → query='MembershipClass', location='iznik-server')\n\nOutput EXACTLY this JSON (proposedTransition MUST be null — this keeps us here to receive results):\n{\n  \"reasoning\": \"<bug summary + what you are about to search for and why>\",\n  \"contextUpdates\": {},\n  \"actions\": [\n    {\"action\": \"check_existing_prs\", \"params\": {\"bugDescription\": \"<excerpt/summary>\", \"extraKeywords\": [\"<component>\"]}},\n    {\"action\": \"search_code\", \"params\": {\"query\": \"<component/function>\", \"location\": \"<directory>\"}}\n  ],\n  \"proposedTransition\": null\n}\n\n═══════════════════════════════════════════════════════════\nPHASE 2 (DIAGNOSE) — execute this if context._action_search_code IS set\n═══════════════════════════════════════════════════════════\n\nEvidence is available:\n  context._action_search_code        — code search results\n  context._action_check_existing_prs — any related open PRs\n\nAlso check:\n  context._action_work_router_decide.dbOpenBugs[0].prRejections — previous fix attempts\n  context.diagnosisMismatchReason — if set, a previous reproduction attempt failed; the new rootCause MUST address this\n\n1. EXISTING-PR CHECK: Read _action_check_existing_prs.existingPRs. If a related open PR already addresses the rootCause, set proposedTransition to \"WORK_ROUTER\" and add contextUpdates.deferReason explaining the duplication.\n\n2. SYMBOL-LEVEL LOCALIZATION: from _action_search_code results, identify the specific function/method name, file path, and line number where the bug most likely resides. Record as: affected_function: '<FunctionName> in <file>:<line>' (e.g. 'DeleteStdMsg in message/stdmsgs.go:142').\n\n3. MULTI-HYPOTHESIS STEP — list ≥2 distinct mechanisms that could produce this symptom. For each:\n   - State the mechanism in one sentence\n   - Identify what code evidence (from _action_search_code) would confirm or rule it out\n   - Rate probability: high / med / low\n   Only after examining all hypotheses, choose the best-supported one as rootCause.\n\n4. If diagnosisMismatchReason is set: rootCause MUST differ from or specifically address the previous mismatch.\n   If prRejections is non-empty: rootCause MUST differ from or refine the previously rejected fix.\n\nOutput EXACTLY this JSON:\n{\n  \"reasoning\": \"<walk through each hypothesis, the evidence for and against, and why you chose rootCause>\",\n  \"contextUpdates\": {\n    \"diagnosisBrief\": {\n      \"topic\": <number>,\n      \"post\": <number>,\n      \"reporter\": \"<username>\",\n      \"symptom\": \"<one sentence: what the user observes>\",\n      \"rootCause\": \"<one sentence: the specific code-level reason>\",\n      \"primaryConfidence\": \"high|med|low\",\n      \"affected_function\": \"<FunctionName in file:line — primary entry point of the bug>\",\n      \"alternativeHypotheses\": [\n        { \"hypothesis\": \"<mechanism>\", \"ruledOutBy\": \"<evidence that contradicts it>\", \"confidence\": \"low\" }\n      ],\n      \"existingPRs\": \"<from _action_check_existing_prs: list related open PR numbers, or 'none'>\",\n      \"evidence\": [\"<file:line — from _action_search_code that supports rootCause>\"],\n      \"reproductionSteps\": [\"<step 1>\", \"<step 2>\"],\n      \"testStrategy\": \"<how to write a test that fails when the bug is present>\",\n      \"testless\": false\n    },\n    \"pendingBugBatch\": [{\n      \"topic\": <number>,\n      \"post\": <number>,\n      \"user\": \"<singleBug.user ?? singleBug.reporter>\",\n      \"summary\": \"<singleBug.excerpt ?? singleBug.summary>\",\n      \"originalPostText\": \"<singleBug.originalPostText>\",\n      \"reviewerReason\": \"<prRejections[0] ?? null>\",\n      \"rejectedPrNumber\": <dbOpenBugs[0].prNumber ?? null>\n    }]\n  },\n  \"actions\": [],\n  \"proposedTransition\": \"REPRODUCE_BUG\"\n}\n\nSet testless: true only if the bug is pure CSS/visual layout or requires physical hardware that cannot be automated.\nDo NOT call delegate_parallel_tasks. Do NOT write code. Do NOT open a PR.",
      "readActions": [
        "search_code",
        "check_existing_prs"
      ],
      "writeActions": []
    },
    "REPRODUCE_BUG": {
      "description": "TDD pipeline step 2: a sub-agent writes a failing test that reproduces the diagnosed bug, runs it, and emits TEST_FAILED=<output>. If testless, emits TEST_SKIPPED=<reason> instead. Returns to REVIEW_REPRODUCTION.",
      "nodeType": "agent",
      "prompt": "You are dispatching a reproduction agent. The diagnosis brief is in context.diagnosisBrief.\n\nIF context.diagnosisBrief.testless === true:\n  Build ONE task with id 'reproduce-<topic>-<post>':\n  Task: 'This bug cannot be reproduced with an automated test (reason: <diagnosisBrief.testStrategy>). Instead, perform the following code inspection to confirm the diagnosis:\\n  1. Read the files listed in diagnosisBrief.evidence.\\n  2. Confirm that the rootCause described is present in the current code.\\n  3. Emit: TEST_SKIPPED=<one sentence explaining what code evidence confirms the diagnosis>\\n  4. Emit: ANALYSIS_COMPLETE=testless confirmed'\n\nIF context.diagnosisBrief.testless !== true:\n  Build ONE task with id 'reproduce-<topic>-<post>':\n  Task: 'Write a test that reproduces this bug using the AssertFlip strategy.\\n\\n  DIAGNOSIS BRIEF:\\n  Symptom: <diagnosisBrief.symptom>\\n  Root cause: <diagnosisBrief.rootCause>\\n  Affected: <diagnosisBrief.affected_function>\\n  Evidence: <diagnosisBrief.evidence joined by newline>\\n  Test strategy: <diagnosisBrief.testStrategy>\\n\\n  TEST SUITE (use the layer matching codeArea):\\n      go-api → Go test in iznik-server-go/\\n          run: cd /home/edward/FreegleDockerWSL/iznik-server-go && MYSQL_HOST=... go test ./... -run TestYourTestName -v\\n      nuxt → Vitest in iznik-nuxt3/\\n          run: cd /home/edward/FreegleDockerWSL/iznik-nuxt3 && npm run test -- --reporter=verbose YourTest\\n      php → Laravel in iznik-batch/, run via status API: POST http://localhost:8081/api/tests/laravel {\"filter\":\"YourTestClass\"}\\n\\n  ASSERTFLIP — two-step protocol (do NOT skip Step 1):\\n\\n  STEP 1 — Write a test that PASSES on the BUGGY (current) behaviour:\\n    Assert the wrong behaviour IS happening. This documents the bug, not the fix.\\n    Example: bug is 'returns nil instead of error' → assert result == nil.\\n    Run it. It must pass cleanly (no syntax errors, no panics).\\n    Emit: BUGGY_TEST_PASSES=yes  (if it passes)\\n          BUGGY_TEST_PASSES=no — <reason>  (syntax/crash; fix those first, then retry Step 1)\\n\\n  STEP 2 — Invert every assertion:\\n    Flip each assert so the test FAILS on buggy behaviour and will PASS after the fix.\\n    Example: assert result == nil  →  assert result != nil\\n    Run the inverted test. It MUST fail now.\\n    If it still passes: the inversion is wrong — diagnose and fix before continuing.\\n    Emit: TEST_FAILED=<last 30 lines of test output proving it fails>\\n    Emit: TEST_FILE=<relative path to the test file you created or modified>\\n    Emit: TEST_COMMAND=<exact command used to run the test>\\n\\n  PUSH THE FAILING TEST (after STEP 2 confirms it fails — do NOT open a PR):\\n    Branch: fix/<featureArea-slug-from-codeArea>-<topic>-<post>  (e.g. fix/ai-images-9646-1)\\n    Use the SAME naming convention as IMPLEMENT_FIX so it can find this branch.\\n    git fetch origin && git checkout -b fix/<...> origin/master\\n    git add <test file only — nothing else>\\n    git commit -m \"test(<featureArea>): AssertFlip failing test for <symptom>\"\\n    git push origin fix/<...>\\n    git log origin/fix/<...> -1 --format=%H  (verify it landed)\\n    Emit: PUSH_VERIFIED=<sha>\\n    Emit: COMMIT_PUSHED=<sha>\\n    Do NOT open a PR — IMPLEMENT_FIX will add the fix and open the single PR.\\n\\n  Do NOT fix the bug. Push only the test.'\n\nCall delegate_parallel_tasks with this single task.\nAfter it returns, set contextUpdates.reproductionResult = the result (the full result object from delegate_parallel_tasks.results[0]).\nPropose transition to REVIEW_REPRODUCTION.",
      "readActions": [],
      "writeActions": [
        "delegate_parallel_tasks"
      ]
    },
    "REVIEW_REPRODUCTION": {
      "description": "TDD pipeline step 3: Opus reviews the reproduction output. Gate: does the failing test actually exercise the reported symptom? Pass → IMPLEMENT_FIX. Mismatch → back to DIAGNOSE_BUG (re-diagnose, max 2 retries). Testless confirmed → IMPLEMENT_FIX with higher adversarial bar.",
      "nodeType": "agent",
      "prompt": "You are reviewing whether the reproduction step correctly captured the reported bug.\n\nINPUTS:\n  context.diagnosisBrief — the structured diagnosis from DIAGNOSE_BUG\n  context.reproductionResult — the full result object from the reproduce sub-agent\n  context.pendingBugBatch[0] — the original bug report metadata\n\nSTEP 1 — Parse the sub-agent output.\nSearch reproductionResult.stdoutTail for:\n  - TEST_FAILED=<output> (automated test confirmed failing)\n  - TEST_SKIPPED=<reason> (testless path confirmed by code inspection)\n  - TEST_FILE=<path> (which file holds the test)\n  - TEST_COMMAND=<command>\n  If reproductionResult.exitCode !== 0 OR reproductionResult.timedOut === true: treat as MISMATCH (sub-agent failed).\n\nSTEP 2 — Evaluate.\n\nCASE A — TEST_FAILED present:\n  Ask yourself: does this test failure correspond to the symptom the user reported?\n  The test must exercise the trigger described in diagnosisBrief.reproductionSteps, not just any failure in the vicinity.\n  If YES → verdict: 'pass'\n  If NO → verdict: 'mismatch', reason: '<what the test exercises vs what the bug report describes>'\n\nCASE B — TEST_SKIPPED present:\n  Ask yourself: is the code inspection evidence cited in TEST_SKIPPED sufficient to confirm the root cause without a test?\n  If YES → verdict: 'testless-confirmed'\n  If NO → verdict: 'mismatch', reason: '<what additional evidence would be needed>'\n\nCASE C — Neither present, or sub-agent failed/timed out:\n  verdict: 'mismatch', reason: 'sub-agent did not emit TEST_FAILED or TEST_SKIPPED'\n\nSTEP 3 — Check retry budget.\n  context.reproductionAttempts (default 0). If verdict === 'mismatch' AND reproductionAttempts >= 2:\n    Override verdict to 'deferred', reason: 'exceeded 2 reproduction attempts — needs manual investigation'\n\nSTEP 4 — Set contextUpdates:\n  contextUpdates.reproductionVerdict = { verdict, reason (if mismatch/deferred), testFile, testCommand, testOutput }\n  If verdict === 'mismatch': contextUpdates.reproductionAttempts = (context.reproductionAttempts ?? 0) + 1\n  If verdict === 'deferred': append { ...pendingBugBatch[0], outcome: 'deferred', reason } to contextUpdates.bugsFixed, set contextUpdates.currentBug = null\n\nPropose transition:\n  pass or testless-confirmed → IMPLEMENT_FIX\n  mismatch (attempts < 2) → DIAGNOSE_BUG (re-diagnose with the mismatch reason as additional context — set contextUpdates.diagnosisMismatchReason = reason)\n  deferred → WORK_ROUTER",
      "readActions": [],
      "writeActions": []
    },
    "IMPLEMENT_FIX": {
      "description": "TDD pipeline step 4: a sub-agent receives the diagnosis brief and failing test, implements the fix until the test passes, then pushes a PR. Returns to VERIFY_DISCOURSE_BATCH for adversarial review.",
      "nodeType": "agent",
      "prompt": "You are dispatching the implementation agent. The diagnosis and reproduction are verified — now fix the bug TDD-style.\n\nINPUTS:\n  context.diagnosisBrief — the verified root cause (includes alternativeHypotheses, existingPRs, primaryConfidence)\n  context.reproductionVerdict — includes testFile, testCommand, testOutput (or testless-confirmed)\n  context.pendingBugBatch[0] — bug metadata\n  Previous rejection reason (if any): context.pendingBugBatch[0].reviewerReason\n\nBuild ONE task with id 'fix-<topic>-<post>':\n\nTask: 'STEP 0 — GIT HISTORY CHECK (do this before writing any code):\\n  git log --oneline --since=\"90 days ago\" -- <primary file path(s) from diagnosisBrief.evidence>\\n  Look for previous fix attempts or reverted patches in this area. If found, note how your approach avoids the same pitfall.\\n  Emit: GIT_HISTORY=<one-line summary, or \"no relevant history\">\\n\\nFix the following verified bug using TDD. Do NOT re-diagnose — the root cause is confirmed.\\n\\n  DIAGNOSIS BRIEF:\\n  Symptom: <diagnosisBrief.symptom>\\n  Root cause: <diagnosisBrief.rootCause> (confidence: <diagnosisBrief.primaryConfidence>)\\n  Evidence: <diagnosisBrief.evidence joined by newline>\\n  Alternatives already ruled out:\\n  <diagnosisBrief.alternativeHypotheses formatted as: \"- <hypothesis>: ruled out because <ruledOutBy>\">\\n  Related open PRs: <diagnosisBrief.existingPRs>\\n\\n  <IF testless-confirmed>\\n  This bug has no automated test (testless). Fix the root cause as described. After fixing, manually verify by reading the changed code and confirming the fix addresses the root cause. Emit: FIX_VERIFIED=testless — describe how the code change addresses the root cause.\\n  </IF>\\n\\n  <IF test present>\\n  FAILING TEST:\\n    File: <reproductionVerdict.testFile>\\n    Command: <reproductionVerdict.testCommand>\\n    Failure output (confirm this still fails before you touch the implementation):\\n    <reproductionVerdict.testOutput>\\n\\n  TASK:\\n  1. Run the test. Confirm it still fails. Emit: CONFIRMED_FAILING=yes\\n  2. Fix the production code (NOT the test) to make the root cause go away.\\n  3. Run the test again. It must pass. Emit: TEST_PASSED=<last 20 lines of output>\\n  4. Run the full test suite for this layer to check for regressions:\\n       go-api: cd /home/edward/FreegleDockerWSL/iznik-server-go && go test ./...\\n       nuxt: cd /home/edward/FreegleDockerWSL/iznik-nuxt3 && npm run test\\n       php: POST http://localhost:8081/api/tests/laravel {}\\n     Emit: SUITE_PASSED=yes (or SUITE_FAILED=<summary of failures>)\\n  5. If SUITE_FAILED: fix the regressions before pushing. Do not push with a red suite.\\n  </IF>\\n\\n  PRE-COMMIT ADVERSARIAL REVIEW — answer each point explicitly in your output BEFORE committing:\\n  □ Root cause addressed: does the fix change the code path identified in the evidence, not just hide the symptom?\\n  □ Other call sites: grep for the pattern fixed (e.g. the function name, the misused API, the wrong constant). List any other files with the same bug.\\n  □ Regression risk: read adjacent tests. Does any existing test break because of this change?\\n  □ Security: path traversal, SQL injection, nil dereference, auth bypass, unvalidated user input reaching a write path?\\n  □ Duplication: is there already a helper/utility in the codebase that does what this fix implements? If yes, use it instead.\\n  □ Dead code: are there any leftover debug statements, unused variables, or commented-out blocks?\\n  □ Conflicts: search for any related open PR that this might conflict with or duplicate.\\n      gh pr list --repo Freegle/Iznik --state open --search \"<featureArea keyword>\"\\n  For each point above, write one line of output: OK or ISSUE: <description>.\\n  If any ISSUE line mentions a real problem (not just a note), fix it before committing.\\n\\n  SCOPE CONSTRAINT — ONE BRANCH, ONE PR:\\n  You are fixing exactly this one bug. Do NOT create branches or open PRs for any other issue you notice.\\n  You must open AT MOST ONE PR (on the branch specified below). If you accidentally open extra PRs, close them before emitting your output marker:\\n    gh pr close <extra-pr-number> --repo Freegle/Iznik --comment \"Closed: opened outside scope of assigned fix\"\\n\\n  BRANCH AND PR:\\n  IF context.pendingBugBatch[0].rejectedPrNumber is set (re-fix of a previously rejected PR):\\n    - Do NOT create a new branch or open a new PR. Push to the EXISTING branch.\\n    - Get the branch: gh pr view <rejectedPrNumber> --json headRefName -R Freegle/Iznik | jq -r .headRefName\\n    - Checkout: git -C /home/edward/FreegleDockerWSL checkout <branch> && git pull origin <branch>\\n    - Commit message: fix(<featureArea>): <one-line description> (re-attempt)\\n    - Push: git -C /home/edward/FreegleDockerWSL push origin <branch>\\n    - Emit: COMMIT_PUSHED=<sha>\\n    - Emit: PR_NUMBER=<rejectedPrNumber>\\n  ELSE (no prior rejected PR, fresh fix):\\n    - Branch: fix/<featureArea-slug>-<topic>-<post> (e.g. fix/stdmsg-delete-9518-217)\\n    - REPRODUCE_BUG already pushed the failing test to this branch. Check it out:\\n        git fetch origin\\n        git checkout fix/<featureArea-slug>-<topic>-<post>\\n        git pull origin fix/<featureArea-slug>-<topic>-<post>\\n      Do NOT use -b (the branch already exists on origin from REPRODUCE_BUG).\\n      If the branch does not exist (testless path or reproduce step failed), create it fresh:\\n        git fetch origin && git checkout -b fix/<featureArea-slug>-<topic>-<post> origin/master\\n    - Commit message: fix(<featureArea>): <one-line description of the root cause fix>\\n    - Open a PR against master. PR body MUST use this exact template:\\n\\n        ## Root Cause\\n        <rootCause from diagnosisBrief — the specific code-level reason, not the symptom>\\n\\n        ## Evidence\\n        <Paste the failing test output from the CONFIRMED_FAILING step: this is the proof the bug existed before the fix>\\n\\n        ## Fix\\n        <What the code change does and why it addresses the root cause>\\n\\n        ## Alternatives Investigated\\n        <List the alternativeHypotheses from diagnosisBrief, each with its ruling-out evidence. Can be brief.>\\n\\n        ## Other Call Sites\\n        <Result of the grep from the pre-commit review: list any other files with the same pattern, or state \"No other occurrences found\">\\n\\n        ## Test\\n        File: <reproductionVerdict.testFile>\\n        Command: <reproductionVerdict.testCommand>\\n        Proves: test FAILS before this commit (see Evidence above), PASSES after (see TEST_PASSED output)\\n        Regression suite: <suite> — SUITE_PASSED=yes\\n\\n        ## Discourse\\n        https://discourse.ilovefreegle.org/t/<topic>\\n\\n    - Emit: COMMIT_PUSHED=<sha>\\n    - Emit: PR_NUMBER=<n>'\n\nCall delegate_parallel_tasks with this single task.\nAfter it returns:\n  Set contextUpdates.bugBatchResults = { results: [<the result>] }\n  Keep contextUpdates.pendingBugBatch as-is (VERIFY_DISCOURSE_BATCH reads it).\nPropose transition to VERIFY_DISCOURSE_BATCH.",
      "readActions": [],
      "writeActions": [
        "delegate_parallel_tasks"
      ]
    },
    "VERIFY_DISCOURSE_BATCH": {
      "description": "TDD pipeline step 5: adversarial review of the implemented fix. Receives the PR number from IMPLEMENT_FIX and the reproduction verdict from context. Calls adversarial_review_pr, records bugsFixed. Discourse reply drafts are NOT queued here — queued automatically by queue_deployed_reply_drafts during LOAD_STATE once deployed.",
      "nodeType": "agent",
      "prompt": "context.bugBatchResults.results[] contains one entry — the result from IMPLEMENT_FIX's delegate. context.pendingBugBatch[] has the bug metadata. context.reproductionVerdict has the test proof.\n\nProcess the single result:\n  - bug = context.pendingBugBatch[0]\n  - D = context.bugBatchResults.results[0]\n  - verdict = context.reproductionVerdict\n\n  SUCCESS: D.exitCode === 0 AND D.prNumber is a number AND D.timedOut !== true:\n    (A) Call create_pr with { prNumber: D.prNumber, topic: bug.topic, post: bug.post, reporter: bug.user ?? bug.reporter, excerpt: bug.excerpt ?? bug.summary }. Passing topic+post is MANDATORY so the bug is immediately marked fix-queued in the DB — omitting them causes the next iteration to re-dispatch the same bug.\n    (B) Call close_extra_prs with { expectedPrNumber: D.prNumber, iterationStartTs: context.iterationStartTs }. This closes any PRs opened outside the scope of the assigned fix task.\n    (C) Call adversarial_review_pr with { prNumber: D.prNumber, repo: 'Freegle/Iznik' }.\n        The adversarial reviewer must additionally check:\n        - If verdict.verdict === 'pass': does the PR diff touch the code path identified in diagnosisBrief.evidence? Does it plausibly make the failing test pass?\n        - If verdict.verdict === 'testless-confirmed': is there clear code evidence the root cause is fixed, even without a test?\n        - SUITE_FAILED in the implementation output is a blocker — do not pass a PR with known regressions.\n    (D) If review passed: do NOT call post_discourse_reply_draft here. The reply draft is automatically queued by queue_deployed_reply_drafts during LOAD_STATE once the fix is confirmed deployed to the production branch. Append { ...bug, prNumber: D.prNumber, frontendOnly: create_pr result.frontendOnly, outcome: 'fixed' } to contextUpdates.bugsFixed.\n    (E) If review failed (blockers): Append { ...bug, outcome: 'deferred', reason: 'adversarial review blocked: ' + review.summary.slice(0,80) } to contextUpdates.bugsFixed.\n\n  FAILURE: D.exitCode !== 0 OR D.prNumber missing OR D.timedOut === true:\n    Do NOT call create_pr.\n    Append { ...bug, outcome: 'deferred', reason: D.timedOut ? 'delegate timed out — re-run next iteration' : (D.failedReason ?? 'delegate failed') } to contextUpdates.bugsFixed.\n\nAfter processing, set contextUpdates.currentBug = null. Reset contextUpdates.diagnosisBrief = null, contextUpdates.reproductionVerdict = null, contextUpdates.reproductionAttempts = 0.\nPropose transition to WORK_ROUTER.",
      "readActions": [],
      "writeActions": [
        "create_pr",
        "close_extra_prs",
        "adversarial_review_pr"
      ]
    },
    "FIX_SENTRY_ISSUE": {
      "description": "Fix one Sentry issue. Feature branch + PR.",
      "nodeType": "agent",
      "prompt": "Pick one actionable Sentry issue from context.sentryIssues. (1) Call read_sentry_issues for the detail. (2) Call search_code to locate the code. (3) Call delegate_to_coder with a task to diagnose, fix, push branch, open PR. (4) Call create_pr to verify. (5) Set contextUpdates.sentryFixAttempted = true. Propose transition to WORK_ROUTER.",
      "readActions": [
        "read_sentry_issues",
        "search_code"
      ],
      "writeActions": [
        "delegate_to_coder",
        "create_pr"
      ]
    },
    "REBASE_DIRTY_PRS": {
      "description": "Rebase all @me open PRs with mergeStateStatus=DIRTY onto master. One delegate per dirty PR in parallel. Returns to COVERAGE_GATE so CI can re-verify after the force-push.",
      "nodeType": "agent",
      "prompt": "context._action_coverage_gate_decide.dirtyPRs[] lists PRs that need a rebase. For each, build a task: 'Rebase PR #<n> (branch <branch>) onto master. Steps: (1) git -C /home/edward/FreegleDockerWSL checkout <branch> && git fetch origin master && git rebase origin/master. If rebase succeeds: git push --force-with-lease origin HEAD. If conflicts arise: git rebase --abort, then diagnose and resolve manually before pushing. Emit COMMIT_PUSHED=<sha> when done.' Use id: 'rebase-<n>' for each. Call delegate_parallel_tasks with all rebase tasks. After it returns, propose transition to COVERAGE_GATE.",
      "readActions": [],
      "writeActions": [
        "delegate_parallel_tasks"
      ]
    },
    "COVERAGE_GATE": {
      "description": "Structural gate: has this iteration produced a PR yet, and does any @me open PR currently have red CI or need rebase? Tool node — deterministic branching, no LLM turn. The single readAction coverage_gate_decide returns _transition: CI_ROUTER | REBASE_DIRTY_PRS | WRAP_UP | WRITE_COVERAGE.",
      "nodeType": "tool",
      "readActions": [
        "coverage_gate_decide"
      ],
      "writeActions": []
    },
    "WRITE_COVERAGE": {
      "description": "Mandatory coverage PR. Single purpose: produce one substantial coverage PR that exhausts the target module.",
      "nodeType": "agent",
      "prompt": "No bug-fix PR was created this iteration. You MUST pick the next coverage target by strict rotation: Go → Vitest → Laravel → Go → …\n\nSTEP 1. Check what was last covered:\n  sqlite3 /home/edward/FreegleDockerWSL/monitor-fsm/monitor.db \"SELECT value FROM kv WHERE key='last_coverage_target'\"\n  Rotation order: Go → Vitest → Laravel → Go. If last was 'Go', pick 'Vitest'. If last was 'Vitest', pick 'Laravel'. If last was 'Laravel' or blank, pick 'Go'.\n\nSTEP 2. Record the choice BEFORE delegating:\n  sqlite3 /home/edward/FreegleDockerWSL/monitor-fsm/monitor.db \"INSERT INTO kv(key,value) VALUES('last_coverage_target','<chosen>') ON CONFLICT(key) DO UPDATE SET value=excluded.value\"\n\nSTEP 3. Call search_code to identify a module with low coverage or untested public functions. Prefer modules recently touched by a bug fix or that have many code paths with no test.\n\nSTEP 4. Call delegate_to_coder with a task that exhausts test coverage for the chosen module. The task must include this instruction:\n\n  'Write comprehensive tests for <module path>. This PR MUST NOT change any production code — test files only.\n\n  COVERAGE MANDATE — do not stop after covering one path. For every public function in the module:\n    1. Read the function.\n    2. List every branch, error path, and edge case (nil input, empty slice, boundary value, concurrent call if relevant).\n    3. For each uncovered path, write a test.\n\n  Use table-driven / parameterised tests wherever the same function is called with different inputs.\n  One table with 10 rows is better than 10 separate test functions — fewer names to invent, easier to add cases.\n\n  Suite-specific setup:\n    Go:      pure unit tests in iznik-server-go/, no Docker/DB required.\n             Run: cd /home/edward/FreegleDockerWSL/iznik-server-go && go test ./... -coverprofile=/tmp/coverage.out ./path/to/pkg/...\n             Report: go tool cover -func=/tmp/coverage.out | grep <pkg>\n    Vitest:  component or composable tests in iznik-nuxt3/.\n             Run: cd /home/edward/FreegleDockerWSL/iznik-nuxt3 && npm run test -- --coverage\n    Laravel: feature or unit tests in iznik-batch/.\n             Run via status API: POST http://localhost:8081/api/tests/laravel {}\n\n  Before writing tests: record the coverage percentage for the module.\n  After writing tests: re-run with coverage. Report the delta.\n  If the delta is < 5% after covering all reachable paths, state that coverage was already high.\n\n  If any test reveals a latent bug (test fails on current code for a real bug, not a test error):\n    - Do NOT fix the production code in this PR.\n    - Mark the test with a skip and a TODO comment: // TODO: latent bug — <one-line description>\n    - Note the latent bug in the PR description for a future fix iteration.\n\n  PR body must use this template:\n\n    ## Coverage added\n    Module: `<path>`\n    Coverage before: <X>%\n    Coverage after: <Y>%\n    Delta: +<Z>%\n\n    ## Tests added\n    | Function | Scenario | File:Line |\n    |---|---|---|\n    | ... | ... | ... |\n\n    ## Latent bugs found (if any)\n    | Function | Description |\n    |---|---|\n    | ... | ... |\n\n    ## No production code changed\n    All changes are in test files only (`*_test.go` / `*.test.ts` / `*Test.php`).\n\n  Emit: PR_NUMBER=<n>'\n\nSTEP 5. When delegate returns with a prNumber: (1) Call adversarial_review_pr. (2) If passed, call create_pr and propose transition to COVERAGE_GATE. (3) If failed with blockers, set contextUpdates.coveragePRReviewFailed = { prNumber, issues } and propose transition to COVERAGE_GATE anyway.",
      "readActions": [
        "search_code"
      ],
      "writeActions": [
        "delegate_to_coder",
        "adversarial_review_pr",
        "create_pr"
      ]
    },
    "WRAP_UP": {
      "description": "Write the iteration summary to /tmp/freegle-monitor/summary.md. Tool node — summary is composed from context + DB, no LLM turn.",
      "nodeType": "tool",
      "readActions": [
        "compose_and_write_summary"
      ],
      "writeActions": []
    },
    "SCHEDULE_NEXT": {
      "description": "Schedule next wakeup. Tool node — delay chosen by rule (≤270s if watching PR CI, else 1500s idle).",
      "nodeType": "tool",
      "readActions": [
        "schedule_next_auto"
      ],
      "writeActions": []
    },
    "END": {
      "description": "Iteration complete.",
      "nodeType": "end"
    }
  },
  "transitions": [
    {
      "id": "t_load_to_ci",
      "from": "LOAD_STATE",
      "to": "CHECK_CI",
      "trigger": "llm_decision",
      "label": "state loaded"
    },
    {
      "id": "t_ci_to_ci_router",
      "from": "CHECK_CI",
      "to": "CI_ROUTER",
      "trigger": "llm_decision",
      "label": "ci checked"
    },
    {
      "id": "t_ci_router_to_drain",
      "from": "CI_ROUTER",
      "to": "WRAP_UP",
      "trigger": "tool_transition",
      "label": "drain mode — CI pending on open PRs"
    },
    {
      "id": "t_ci_router_to_master_fix",
      "from": "CI_ROUTER",
      "to": "FIX_MASTER_CI",
      "trigger": "llm_decision",
      "label": "master red"
    },
    {
      "id": "t_ci_router_to_parallel",
      "from": "CI_ROUTER",
      "to": "PARALLEL_ANALYZE_AND_FIX",
      "trigger": "llm_decision",
      "label": "master green — dispatch PRs + topics + sentry in parallel"
    },
    {
      "id": "t_parallel_to_collate",
      "from": "PARALLEL_ANALYZE_AND_FIX",
      "to": "COLLATE_RESULTS",
      "trigger": "llm_decision",
      "label": "all parallel agents finished"
    },
    {
      "id": "t_collate_to_work_router",
      "from": "COLLATE_RESULTS",
      "to": "WORK_ROUTER",
      "trigger": "llm_decision",
      "label": "results collated"
    },
    {
      "id": "t_master_fix_to_ci",
      "from": "FIX_MASTER_CI",
      "to": "CHECK_CI",
      "trigger": "llm_decision",
      "label": "master fix attempted, re-verify"
    },
    {
      "id": "t_work_router_to_diagnose",
      "from": "WORK_ROUTER",
      "to": "DIAGNOSE_BUG",
      "trigger": "llm_decision",
      "label": "bug pending — begin TDD pipeline"
    },
    {
      "id": "t_diagnose_to_reproduce",
      "from": "DIAGNOSE_BUG",
      "to": "REPRODUCE_BUG",
      "trigger": "llm_decision",
      "label": "diagnosis brief written"
    },
    {
      "id": "t_reproduce_to_review",
      "from": "REPRODUCE_BUG",
      "to": "REVIEW_REPRODUCTION",
      "trigger": "llm_decision",
      "label": "reproduction agent finished"
    },
    {
      "id": "t_review_pass_to_implement",
      "from": "REVIEW_REPRODUCTION",
      "to": "IMPLEMENT_FIX",
      "trigger": "llm_decision",
      "label": "reproduction verified — fix it"
    },
    {
      "id": "t_review_mismatch_to_diagnose",
      "from": "REVIEW_REPRODUCTION",
      "to": "DIAGNOSE_BUG",
      "trigger": "llm_decision",
      "label": "reproduction mismatch — re-diagnose"
    },
    {
      "id": "t_review_deferred_to_work_router",
      "from": "REVIEW_REPRODUCTION",
      "to": "WORK_ROUTER",
      "trigger": "llm_decision",
      "label": "reproduction exhausted retries — deferred"
    },
    {
      "id": "t_implement_to_verify",
      "from": "IMPLEMENT_FIX",
      "to": "VERIFY_DISCOURSE_BATCH",
      "trigger": "llm_decision",
      "label": "implementation pushed — adversarial review"
    },
    {
      "id": "t_verify_batch_to_work_router",
      "from": "VERIFY_DISCOURSE_BATCH",
      "to": "WORK_ROUTER",
      "trigger": "llm_decision",
      "label": "batch processed"
    },
    {
      "id": "t_work_router_to_sentry_fix",
      "from": "WORK_ROUTER",
      "to": "FIX_SENTRY_ISSUE",
      "trigger": "llm_decision",
      "label": "sentry pending"
    },
    {
      "id": "t_work_router_to_gate",
      "from": "WORK_ROUTER",
      "to": "COVERAGE_GATE",
      "trigger": "llm_decision",
      "label": "queue empty"
    },
    {
      "id": "t_sentry_fix_to_work_router",
      "from": "FIX_SENTRY_ISSUE",
      "to": "WORK_ROUTER",
      "trigger": "llm_decision",
      "label": "sentry fix done"
    },
    {
      "id": "t_gate_to_rebase",
      "from": "COVERAGE_GATE",
      "to": "REBASE_DIRTY_PRS",
      "trigger": "llm_decision",
      "condition": "dirtyPRs non-empty AND redPRs empty",
      "label": "dirty PRs need rebase — fix before coverage"
    },
    {
      "id": "t_rebase_to_gate",
      "from": "REBASE_DIRTY_PRS",
      "to": "COVERAGE_GATE",
      "trigger": "llm_decision",
      "label": "rebase attempts done — re-check gate"
    },
    {
      "id": "t_gate_to_wrap",
      "from": "COVERAGE_GATE",
      "to": "WRAP_UP",
      "trigger": "llm_decision",
      "condition": "verify_pr_created count > 0 AND redPRs empty",
      "label": "gate passed"
    },
    {
      "id": "t_gate_to_cov",
      "from": "COVERAGE_GATE",
      "to": "WRITE_COVERAGE",
      "trigger": "llm_decision",
      "condition": "verify_pr_created count == 0",
      "label": "gate failed"
    },
    {
      "id": "t_gate_to_ci_router",
      "from": "COVERAGE_GATE",
      "to": "CI_ROUTER",
      "trigger": "llm_decision",
      "condition": "redPRs non-empty",
      "label": "open PR red — must fix first (fall back to phase A)"
    },
    {
      "id": "t_cov_to_gate",
      "from": "WRITE_COVERAGE",
      "to": "COVERAGE_GATE",
      "trigger": "llm_decision",
      "label": "coverage PR pushed"
    },
    {
      "id": "t_wrap_to_sched",
      "from": "WRAP_UP",
      "to": "SCHEDULE_NEXT",
      "trigger": "llm_decision",
      "label": "wrap-up done"
    },
    {
      "id": "t_sched_to_end",
      "from": "SCHEDULE_NEXT",
      "to": "END",
      "trigger": "llm_decision",
      "label": "wakeup scheduled"
    }
  ]
}
