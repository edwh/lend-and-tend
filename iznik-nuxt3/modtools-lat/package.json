{
  "name": "iznik-modtools-lat",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxt dev --port 4003",
    "build": "nuxi prepare && nuxt build",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "clean": "nuxi cleanup"
  }
}
